# Models Folder

Trained model artefacts are saved here after running the notebooks.

| File | Description |
|---|---|
| `best_model.pkl` | Best-performing classifier (serialised with pickle) |
| `scaler.pkl` | Fitted StandardScaler used during preprocessing |

These files are generated at runtime.  
They are excluded from version control via `.gitignore`.
